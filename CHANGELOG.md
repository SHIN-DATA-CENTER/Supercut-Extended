# 変更履歴

## v1.3.3

### 編集画面の不具合修正

- **末尾のクリップが再生できず、プレビューが黒くなったまま戻らなくなる問題を修正。**
  シーケンスの最後まで再生すると、動画の読み込み自体を解除していた。以降はどのクリップを
  選んでも「同じ動画が読み込み済み」と判定されて読み直さないため、何も再生できなくなっていた。
  最後まで再生したら**一時停止するだけ**にした
- **ウィンドウの横幅によって黒帯が復活する問題を修正。** 黒帯をカットしたとき、
  カットしたはずの部分が出力枠の**外側にはみ出して描かれていた**。
  `setSceneRect` は「どこまでスクロールするか」を決めるだけで描画範囲は制限しないため。
  出力枠でクリップするようにした
- **編集画面の再生時間が元動画のものになっていた問題を修正。** 33 分の録画から 5 分の
  montage を作っていても `33:13` と出ていて、作っているものの長さが読めなかった。
  再生時間・シークバーとも**シーケンス側**を表示する。シークバーを掴んだときも
  シーケンス上の位置へ飛ぶ

### 編集画面の操作性

- 拡大・縮小のボタンが文字の `+` `-` で潰れて読めなかったのをアイコンにした
- **Alt+ホイールでタイムラインを左右に移動**できるようにした
- ショートカットの説明に `Ctrl+ホイール=拡大縮小` と `Alt+ホイール=左右に移動` を追記

### 編集ツールをアイコン化し、タイムラインの左に縦に並べた

「選択」「カット」がテキストのボタンだったのを、アイコンだけの正方形ボタンにして、
タイムラインの左側に縦一列で置いた。編集ソフトのツールパレットと同じ配置。

- **選択 (V)** / **カット (C)** … 押している間だけ有効な「モード」。選択中はアクセント色で塗る
- 区切り線
- **複製 (Ctrl+D)** / **削除 (Del)** … 選択中のクリップに対する一回きりの操作

モード（クリックしたときの挙動が変わるもの）と一回きりの操作を線で分けてある。
「元に戻す」「やり直す」もアイコンにして、上の行へ移した。

ポインタとハサミの絵柄だけはアイコンセットに無かったので自前で描いた。
同梱のセット（`cooliocns SVG/`）に混ぜず、`icons.py` にソースとして持っている。
あの一式は第三者のもので、かつリポジトリに含めていないため。

### プレビューに YouTube 風のシークバーを追加

メインウィンドウと編集画面、どちらのプレビューにも付く。

- 普段は細く、ポインタを乗せると太くなってつまみが出る（アニメーションする）
- ポインタの位置までが薄く塗られ、どこへ飛ぶか分かる
- ドラッグ中は再生位置の更新を無視する。そうしないと、掴んでいるつまみが
  再生の進行に引っ張られて手元から逃げる

### プレビューの余計な黒帯をやめた

プレビューの映像は、ウィンドウの形とは基本的に一致しない。これまでは余った領域を
**黒で塗っていた**ので、映像にも出力にも存在しない帯が見えていた。
塗るのをやめ、ウィンドウの地色がそのまま出るようにした。

**出力に実際に入る黒帯だけは、これまでどおり黒で描く。** 出力解像度を指定していて
縦横比が合わない場合、その余白は本当にファイルに書き込まれるため。

> 録画そのものに焼き込まれた黒帯（4:3 のゲーム画面が 16:9 の録画に入っている場合）は
> 映像の一部なので、これでは消えない。`黒帯を自動検出` からカットすること。

### 配布形態

これまでの「exe 1 本で全部入り」から、**`SupercutExtended` フォルダごと配布する形**に変えた。
zip を展開すると、2 つの exe と `_internal\` が入ったフォルダが出てくる。

同じソース・同じ PC での実測:

| | 単体 exe（v1.3.2 まで） | フォルダ形式（v1.3.3〜） |
|---|---|---|
| 配布 zip | 227.7 MB | **120.4 MB** |
| 起動・初回 | 9.3 秒 | 6.1 秒 |
| 起動・2 回目以降 | 9.3 秒 | **2.0 秒** |
| 展開後 | exe 2 本で 228.6 MB | フォルダ 303.9 MB |
| 同梱 ffmpeg | 2 部（exe ごと） | **1 部** |

**起動が速くなる理由**は、単体 exe が起動のたびに中身を `%TEMP%` へ展開していたから。
同梱した ffmpeg（204 MB）がそのまま毎回の起動時間に乗るので、**何回起動しても
速くならなかった**。フォルダ形式は展開が無く、2 回目以降は OS のキャッシュが効く。

**zip が半分近くまで縮む理由**は、単体 exe の中身が既に圧縮済みで zip がほとんど
効かないのに対し、フォルダ形式は生のファイルなので普通に圧縮できるから。

**Python のインストールは引き続き不要。** インタプリタ（`python313.dll`）も標準ライブラリも
MSVC ランタイムも `_internal\` に入っている。`python` コマンドが引けず `PYTHONHOME` /
`PYTHONPATH` が壊れた環境でも動くことを確認済み。

**注意点は「フォルダごと扱う」ことだけ。** exe だけ別の場所にコピーすると起動しない。
移動・コピーはフォルダ単位で行うこと。

### 更新について

v1.3.2 の更新処理がフォルダ形式に対応済みなので、**v1.3.2 からは自動更新でそのまま
移行できる**（`_internal\` が無い状態から生成される）。

v1.3.1 以前から更新する場合は、いったん v1.3.2 を経由するか、リリースページから
手動でダウンロードすること。

## v1.3.2

見た目の変化はない。次のバージョンで配布形態をフォルダ形式（onedir）に変えるための
下ごしらえと、リポジトリの整理。

### フォルダ配布への更新に備えて、更新処理を作り直した

**この修正は「更新される側」ではなく「更新する側」に入っている。** つまり
フォルダ配布に切り替えるより前に、いったんこのバージョンを挟んでおく必要がある。
更新を実行するのは常に**今動いている古い方**のアプリなので、v1.3.1 のまま
フォルダ配布へ飛ぶと、古い更新処理が後始末をできないまま入れ替えることになる。

具体的には、入れ替えの `robocopy` を 2 つに分けた。

- `_internal\`（PyInstaller が全部を所有する実行時フォルダ）は `/PURGE` を付けて**同期**する。
  付けないと、過去のバージョンが置いた `.pyd` や Qt プラグイン DLL が消えずに残り続ける。
  PySide6 を上げた後に古いプラグインが残っていると、起動時の import エラーや
  プラグイン重複でクラッシュする
- アプリ本体のあるフォルダ直下は**これまで通り上書きのみ**で、`/PURGE` はかけない。
  ここにはユーザー自身が置いたファイルがあり得るので、消してはいけない

単体 exe の zip が来た場合は `_internal\` が無いので、その処理は丸ごと飛ばす。
単体 exe 版からフォルダ版への移行も、その逆も、どちらも通る。

### 添付ファイルの選び方を修正

updater はリリースの添付ファイルから「**最初に見つけた zip**」を掴んでいた。
zip を 1 つしか付けていなかったので偶然動いていただけで、2 つ目（CLI 用など）を
付けた時点で、GitHub が返す順番次第で中身の違う zip を掴む可能性があった。
`SupercutExtended.exe` が入っていない zip を掴むと、ダウンロードと展開まで
進んでから最後で失敗する。

本体らしくない名前（`cli` / `source` / `src` / `debug` などを含むもの）を
後回しにするようにした。候補が全部そう見える場合は、以前と同じく先頭を使う。

### ffmpeg のバイナリを git の追跡から外した

`vendor/ffmpeg/*.exe`（合計 203 MB）は `.gitignore` に書いてあるにもかかわらず
追跡されていた。git は**追跡済みのファイルには .gitignore を適用しない**ため、
別 PC から入った時点で除外が効いていなかった。

追跡を外した。次に ffmpeg を更新しても、もうリポジトリが膨らまない。
クローン直後は `python tools/fetch_ffmpeg.py` で取得する（README 記載の手順どおり）。
過去のコミットとタグには残るので、リポジトリの取得サイズ自体は変わらない。

### `supercut.spec` と `requirements.txt` をリポジトリに含めるようにした

`.gitignore` が両方とも除外していたため、**クローンしただけではビルドできなかった**。
`*.spec` の除外は GitHub の Python 用テンプレートが「spec は自動生成物」と
想定しているためだが、このプロジェクトの spec は手書きのビルド定義なので誤りだった。

## v1.3.1

### 【重要】自動更新が %TEMP% フォルダを丸ごと削除する問題を修正

更新の最終処理で、展開先の一時フォルダを消すつもりが **Windows の一時フォルダ
（`%TEMP%`）そのものを削除**していた。

`stage()` は zip の直下に exe がある場合、展開先フォルダ自身を返す。
後片付けはそれの**親**を消していたので、消える対象が `%TEMP%` になっていた
（zip の中で 1 階層下に入っている場合だけ、たまたま正しく動いていた）。

配布用 zip は exe を直下に置く形なので、**実際に更新すれば必ず踏む**経路だった。
自分で管理していると識別できる名前のフォルダ以外は消さないようにした。

なお v1.3.0 以前ではこの後片付けまで到達していない（下記の WinError 87 で先に
失敗していた）ため、実害が出た人はいないはず。

### 自動更新が必ず失敗していた問題を修正

「更新する」を押すと `[WinError 87] パラメーターが間違っています。` で止まり、
**これまで一度も自動更新できていなかった**。

原因は入れ替え用バッチを起動するときのプロセス生成フラグ。
`CREATE_NEW_CONSOLE` と `DETACHED_PROCESS` は Win32 の `CreateProcess` で排他で、
同時に渡すと `ERROR_INVALID_PARAMETER`（87）で即失敗する。バッチが起動すら
していなかった。

`CREATE_NEW_CONSOLE` 単独に変更。バッチは進捗を表示し、コピー失敗時は `pause` で
止まる作りなのでコンソールが要る（`DETACHED_PROCESS` だとコンソールが無く、
更新に失敗しても何も見えないまま止まり続けることになる）。

**v1.3.0 以前を使っている場合、この修正自体が自動更新では届きません。**
お手数ですが、リリースページから手動でダウンロードしてください。以降は
アプリ内の「ヘルプ → アップデートを確認」で更新できます。

なお v1.1.1 のリリースは `.exe` を直接添付していたため、そもそも更新対象として
認識されていなかった（updater は `.zip` のアセットしか受け付けない）。
v1.3.1 からは zip で配布する。

## v1.3.0

### 出力フレーミング（解像度・黒帯カット・引き延ばし）

出力セクションに **「画面サイズ・黒帯」** を追加。

- **出力解像度のプリセット**: ソースのまま / FHD / QHD / 4K / HD / 1080x1080（正方形） /
  1080x1920（縦）。`カスタム...` で任意のサイズも指定できる
- **黒帯のカット**（左 / 右 / 上 / 下、ソースのピクセル数で指定）
- **`黒帯を自動検出` ボタン**。プレビュー中の録画から実際の黒帯を測って 4 つの欄に入れる。
  ピクセル数を目測する必要はない。録画の**中盤**を解析する（冒頭は暗いロード画面のことが
  多く、そこを見ると画面全体が黒帯と判定されてしまうため）
- **`画面いっぱいに引き延ばす`**。4:3 のゲーム画面が 16:9 の録画に入っている場合など、
  黒帯をカットした映像を出力解像度いっぱいに広げる。**縦横比は意図的に崩れる** ——
  4:3 を引き延ばしてプレイしている人が求めているのはこの絵だから。
  オフのときは縦横比を保ったまま黒で埋める
- 設定は次回起動時にも保たれる

### プレビューが「出力される範囲」だけを表示するようになった

メインウィンドウと編集画面の両方で、プレビューが**書き出される映像そのもの**になった。
黒帯をカットすればプレビューからも消え、引き延ばせばプレビューも引き延びる。
再生バーの右に出力解像度（例: `1920x1080 · 引き延ばし`）が表示される。

そのために映像の描画を `QVideoWidget` から `QGraphicsScene` + `QGraphicsVideoItem` に
変更した。`QVideoWidget` は「フレーム全体を枠に収める」ことしかできず、
消したいはずの黒帯が書き出しの瞬間まで画面に残り続けるため。

### 所要時間の見積もりが出力解像度を反映するようになった

エンコード時間は**出力解像度でほぼ決まる**。実測すると:

| 出力 | 実測 | 見積もり |
|---|---|---|
| 1440x1080（黒帯カットのみ） | 1.19x | 1.22x |
| 1920x1080（カット＋等倍） | 0.98x | 1.00x |
| 3840x2160（4K へ拡大） | 0.30x | 0.32x |

**黒帯をカットすると画素が減るぶん、むしろ少し速くなる。** 遅くなるのは拡大したときだけ。
デコードなど解像度に依存しない処理が全体の約 29% を占める、というモデルで見積もっている
（実測との誤差は 7% 以内）。

### CLI

- `--size 1920x1080` … 出力解像度
- `--crop 240,240` … 黒帯のカット。`240`（四辺）/ `240,240`（左右）/ `240,240,0,0`（左右上下）
- `--stretch` … 出力解像度いっぱいに引き延ばす（`--size` が必要）

### その他

- **解像度の違う録画どうしを結合できるようになった。** 出力解像度を指定すると全ソースが
  同じフレームに揃うため、これまで「結合できません」と断っていた組み合わせが通る
- フレーミングを使うとデコードは CPU 側になる（同梱 ffmpeg の CUDA フィルタには
  `scale_cuda` はあるが `crop` / `pad` / `setsar` が無い）。エンコード（重い側）は
  GPU のままなので、影響はフェードと同程度

### 既知の制限

- **モーションブラーは未対応。** 同梱 ffmpeg に GPU で動くブラー系フィルタが存在しないため
  （`tmix` / `tblend` / `minterpolate` のいずれにも `_cuda` / `_opencl` / `_vulkan` /
  `_qsv` 版が無いことを確認済み）。CPU 処理では実用的な速度が出ないので見送り

## v1.2.0

### 簡易クリップ編集機能を追加

`編集画面を開く` から**独立ウィンドウ**の編集画面が開く。メインウィンドウは開いたまま
使えるので、試合を切り替えながら編集内容を組み立てられる。

- **タイムライン上でクリップをドラッグして並べ替え**。挿入位置が縦線で表示される
- **クリップの端をドラッグして長さを調整**。数値入力でも指定できる。
  トリムは録画の実尺までで止まる（存在しない映像は指定できない）
- **クリップ単位の取捨選択**。「使用する」のチェックを外すと、並びを保ったまま
  出力から除外される（消さずに試せる）。`削除` で完全に取り除くこともできる
- **フェードイン / フェードアウト**（映像・音声の両方）をクリップごとに指定
- **BGM** を追加できる。音量・フェードイン / フェードアウト・ループに対応
- Ctrl+ホイールでタイムラインを拡大縮小
- **選択ツール / カットツール**（V / C）。カットはクリップを 2 つに分割する
- **元に戻す / やり直す**（Ctrl+Z / Ctrl+Y）。50 段階まで遡れる
- **複製**（Ctrl+D）、**削除**（Del）、**再生/一時停止**（Space）
- ウィンドウは最大化・リサイズでき、サイズと位置を記憶する
- クリップをドラッグすると、掴んだクリップが浮き上がってカーソルに追従する。
  元の位置は破線の空きスロット、落下先は縦線で示す
- **編集ソフト風のレイアウト**。左にクリップ一覧、中央にプレビュー、右にインスペクタ、
  下に全幅タイムライン（タイムルーラー・V1/BGM のトラックヘッダ・プレイヘッド）
- **ルーラーをドラッグしてシーケンス全体をスクラブ**できる。再生位置はタイムコードで表示
- クリップ上にフェードのランプが三角形で描かれる。拡大縮小と「全体表示」ボタン
- **プレビュー**。クリップを選ぶとその位置へ頭出しし、再生すると次のクリップへ
  自動で進むので、書き出す前に並び順と長さを通しで確認できる。赤い縦線が再生位置。
  なおフェードと BGM は書き出し時に適用されるため、プレビューには反映されない

クリップはイベントの色分けを引き継ぐので、キル・エース・勝利などが一目で分かる。

### 実装上の判断

- **クリップは「セグメント 1 つ = 1 クリップ」で作る。** イベント単位ではない。
  `build_segments()` は窓が重なるイベントを既にマージしており、イベント単位にすると
  素材が重複したクリップが並んでしまうため。表示される並びが、そのまま書き出される
- **フェードのあるクリップはデコードだけ CPU に落とす。** `fade` はソフトウェア
  フィルタで、CUDA メモリ上のフレームには適用できない（GPU パスで `-pix_fmt` を
  付けられないのと同じ理由）。`hwdownload`/`hwupload` で往復させるより素直で、
  高コストなエンコード側は GPU のまま
- **フェード・BGM を指定すると「無劣化コピー」から自動的に再エンコードへ切り替わる。**
  コピーはパケットを素通しするので、フェードも音声合成も原理的に入る余地がない。
  黙って効果を落とすより切り替える（画面にもその旨を表示する）
- **BGM 合成では映像を再エンコードしない。** 変わるのは音声グラフだけなので
  映像は `-c:v copy` で通す。2 度目の映像パスの時間も画質劣化も払う理由がない
- ループ指定の BGM が本編より長く伸びないよう、`amix` に `duration=first` を指定

### 内部

- `model.Clip` / `model.Timeline` / `model.Bgm` を新設。`Segment` は
  `build_segments()` が毎回ソート・マージし直すため並べ替えや個別トリムを保持できない。
  `Clip` は「ユーザーが所有する側のコピー」で、トリムは in/out を動かすだけ
- `render.render_timeline()` … 任意順・個別トリム・フェード・BGM に対応
- `gui/clip_timeline.py`（新規）… 編集用タイムライン。既存の `timeline.py` は
  1 本の録画を実時間軸で表示するシークバーで、用途が別物なので分離した
- `gui/editor.py`（新規）… 編集ウィンドウ
- 検証: `tools/test_timeline.py`（レンダリング 14 項目）、
  `tools/test_editor_ui.py`（実際のマウスドラッグを再現して UI 13 項目）

---

## v1.1.1

### プリセットと画質が所要時間の見積もりに反映されるようになった

これまで「予想 ○秒」はエンコーダ（GPU / CPU）だけを見ており、**プリセットや画質を
変えても数字が動かなかった**。実測した係数を使って両方を反映するようにした。

原因は 2 つあった。

- 見積もりの計算がプリセットと画質を参照していなかった
- **プリセットのコンボにシグナルが 1 つも繋がっておらず**、変更しても再計算自体が
  走っていなかった（画質のほうは繋がっていたが、計算が見ていないので動かなかった）

実際の変化（15分41秒 の素材、GPU H.264 の場合）:

| プリセット | 予想 |
|---|---|
| 最速 (p1) | 232 秒 |
| 標準 (p4) | 約 380 秒 |
| 最高画質 (p7) | 787 秒 |

### 見積もりの精度そのものも大きく改善した

係数を入れるにあたって全エンコーダを実測したところ、**元になっていた基準値が
実態と大きくずれていた**ことが判明したので、実際のレンダリングで測り直した。

| | 旧 | 実測 |
|---|---|---|
| NVENC H.264 | 4.3 倍速 | **2.6 倍速** |
| NVENC HEVC | 4.0 倍速 | **2.8 倍速** |
| libx264 | 1.0 倍速 | **2.5 倍速** |
| libx265 | 0.5 倍速 | **1.0 倍速** |

ずれの原因は、**単一クリップの合成ベンチで測っていた**こと。本ツールはセグメントごとに
ffmpeg を起動するため、短いクリップを多数つなぐ montage では起動コストが積み上がる。
そこで実際の 27 セグメント / 55.5 秒のジョブで測り直した。

修正後の誤差（実レンダリングとの比較）:

```
h264_nvenc p4        予想 21秒  実測 21秒  (-0%)
h264_nvenc p7        予想 46秒  実測 47秒  (+2%)
libx264    veryfast  予想 22秒  実測 24秒  (+9%)
```

### 測って分かったこと: GPU では画質を変えても速度は変わらない

NVENC は固定機能なので、**レート制御の目標値（CQ）を変えても処理量がほとんど変わらない**
（cq18〜cq30 で 0.98〜1.01 倍、誤差の範囲）。一方 CPU の CRF はビット数がそのまま
処理量に効くので、libx264 で約 1.27 倍、libx265 で約 1.5 倍の差が出る。

そのため**画質を動かして見積もりが変わるのは CPU を選んだときだけ**で、これは実測に
基づく正しい挙動。GPU 側で無理に数字を動かすことはしていない。

プリセットの効きは大きく、GPU H.264 で p1 が p4 の 1.56 倍、p7 が 0.46 倍。

### その他

- `tools/bench_presets.py`（新規） — アプリが実際に使う引数でプリセット・画質の
  速度係数を実測する。係数は「既定値 = 1.00」に正規化して `encoder.py` に持たせている
- `tools/test_eta.py`（新規） — 見積もりがプリセット・画質に追随することを検証する。
  今回の 2 つの原因はどちらもレンダリングのテストでは検出できなかったため
- `QUALITY_TIERS` / `DEFAULT_QUALITY` を GUI から `encoder.py` へ移動（速度モデルと
  同じ場所で管理するため）

---

## v1.1.0

`feature/any-paturn-encode` の内容を取り込んだリリース。**ffmpeg の同梱**と
**CPU/GPU・コーデックの選択**が入り、v1.0.1 の複数試合まとめ出力と合流した。

### ffmpeg を同梱した

これまでは ffmpeg を別途用意する必要があった（PATH に入れるか、exe の隣に置く）。
**ffmpeg / ffprobe を exe に同梱**したので、**何も入っていない PC でもそのまま動く**。

- 同梱ビルドは gyan.dev の `ffmpeg-8.1.2-essentials_build`（**GPLv3**）。
  libx264 / libx265 / NVENC / AMD AMF / Intel QSV の H.264・HEVC が全部有効な、
  公式 Windows ビルドで唯一の組み合わせ
- ライセンス全文と再配布上の注意は `vendor/ffmpeg/NOTICE.txt` に同梱している
- バイナリは git に入れていない（200 MB 超）。`python tools/fetch_ffmpeg.py` で取得する
  （バージョン固定・SHA256 検証つき）
- 同梱の ffmpeg が使えない場合は PATH、次に `SUPERCUT_FFMPEG` 環境変数へフォールバックする

### エンコーダを「CPU / GPU」と「H.264 / HEVC」で選べるようにした

`h264_nvenc` のような生の ffmpeg 名を並べる代わりに、**2 つの平易な質問**に分けた。
実際に使える組み合わせだけが有効になり、GPU 側には検出したベンダ名（NVIDIA / AMD /
Intel）が出る。画質も CQ の数値ではなく段階から選ぶ形にした。

対応エンコーダに HEVC と CPU 側を追加: `hevc_amf` / `hevc_qsv` / `libx265`。

### 変更・修正

- **同梱した ffmpeg が単体 exe で見つからない問題を修正。** 1 ファイル構成では同梱物は
  `%TEMP%` の展開先に置かれるが、探索が exe の隣と PATH しか見ていなかった。そのままだと
  同梱しているのに PATH へフォールバックし、ffmpeg の無い PC で失敗する
- **残り時間の見積もりがエンコーダを見るようになった。** GPU 4.3 倍速に対し libx264 は
  約 1.0 倍、libx265 は約 0.5 倍と大きく違うため、CPU を選ぶと見積もりも実態に寄る
  （複数試合をまとめるときの見積もりにも同じ係数が効く）
- **README に未解決のコンフリクトマーカーが混入していたのを修正。** 別 PC でのマージが
  解決されないままコミットされていた
- **CLI で試合 ID を短く指定すると、黙って別の試合を書き出すことがあったのを修正。**
  Outplayed の試合 ID は `<セッションのハッシュ>_<連番>` という形で、**同じセッションの
  試合は末尾以外が全部同じ**。先頭数文字だけ渡すと複数に当たるのに、1 件目を無言で選んで
  いた（この PC の実データでは 8 文字の指定が 5 試合に一致した）。曖昧な場合は候補を
  並べて中断するようにした

### 配布形態について

ffmpeg を同梱したことで payload が 204 MB 増えたため、単体 exe と
フォルダ配布の差が v1.0.1 より大きくなっている（`supercut.spec` の `ONEFILE` で切替）。

| 設定 | 配布物 | 起動（GUI が出るまで） |
|---|---|---|
| `ONEFILE = True`（既定） | exe 2 本（各 114 MB） | **約 10.4 秒** |
| `ONEFILE = False` | フォルダ 306 MB（ffmpeg は 1 部だけ） | **約 2.5 秒** |

単体 exe は起動のたびに中身を `%TEMP%` へ展開するため、同梱分がそのまま起動時間に乗る。
フォルダ配布は展開が無いので、同梱しても起動時間は変わらない（初回のみディスクから
読み込むぶん遅く、実測 7.8 秒 → 2 回目以降 2.4 秒）。持ち運びやすさを取るか起動の速さを
取るかで選ぶ。

---

## v1.0.1

### 新機能: 複数の試合をまとめて Supercut

試合一覧の左端に**チェック欄**を追加し、複数の試合をまとめて書き出せるようにした。

- **チェックが 1 つも無いときは、プレビュー中の試合だけが対象**になる。
  1 試合だけ作るときの操作はこれまでと変わらない
- チェック欄と行の選択（プレビュー）は**別扱い**。選択を崩さずに各試合を見て回れる
- チェックは試合 ID で保持しているので、**検索で絞り込んでも消えない**。
  `全選択` / `解除` は表示中の行だけに作用する
- 2 件以上チェックすると「複数選択したとき」の切り替えが有効になる
  - **1本にまとめる** — 選んだ試合を**時系列順**につないで 1 ファイルにする
  - **試合ごとに別ファイル** — 試合ごとに 1 本ずつ書き出す。保存先の指定がフォルダに変わる
- 進捗は**バッチ全体で 0〜100%** を通して表示する（ファイルごとに巻き戻らない）。
  素材の長さで重み付けしているので、長い試合ほど進みがゆっくりになる

CLI も同じことができる:

```bash
supercut build 3 5 8 --events kill      # 3 試合を 1 本にまとめる
supercut build 3 5 8 --separate         # 試合ごとに 1 本ずつ
```

`build` に渡す番号は `list` の行番号だが、**録画が増えると行番号はずれる**。
確実に指定したいときは試合 ID をそのまま渡す（v1.1.0 で曖昧な指定を弾くようにした）。

#### 結合できる録画の条件

2 段目の連結は concat デマクサなので、**パーツのストリームの形が揃っていないと繋げない**。
`encode` はコーデックと（単一トラックを選んだときの）音声は揃えるが、**解像度と fps は
ソースのまま**で、`copy` は何も揃えない。

そこで結合前に照合し、食い違う場合は**エンコードを始める前に**中断して、どのファイルが
どう違うかを表示する（長時間エンコードした末に連結で失敗するのを避けるため）。

```
these recordings cannot be joined into one file because their streams differ:
  1920x1080 60fps 3 audio tracks:  A.mp4, B.mp4
  2560x1440 60fps 3 audio tracks:  C.mp4
```

この場合は「試合ごとに別ファイル」を使うか、条件の揃った録画を選び直す。

### exe が単体で動くようになった

これまでは `SupercutExtended.exe`（2.5 MB）の隣に `_internal\`（106 MB / 693 ファイル）を
置いておく必要があった。**1 ファイルで完結する形**に変更し、どこにコピーしても単体で動く。

| | v1.0.0 | v1.0.1 |
|---|---|---|
| 配布物 | exe + `_internal\` 693 ファイル | **exe 1 本（43 MB）** |
| 起動（GUI が出るまで） | 約 2.5 秒 | 約 9 秒 |

単体 exe は起動のたびに中身を `%TEMP%` へ展開するため、**起動が 6 秒ほど遅くなる**
（実測 4 回平均: 8.95 秒 対 2.52 秒）。起動の速さを優先したい場合は `supercut.spec` 冒頭の
`ONEFILE = True` を `False` にすれば元のフォルダ配布に戻せる。

**ffmpeg は引き続き別途必要**（サイズと GPL 再配布の都合で同梱していない）。PATH に無ければ
`ffmpeg.exe` と `ffprobe.exe` を exe と同じフォルダに置く。

### アプリアイコンを設定

exe・ウィンドウ・タスクバーのアイコンを設定した（同梱アイコンセットの「Edit/Layers」を
16〜256px の 9 サイズで描き起こしたもの）。

- グリフの背後に**角丸のプレート**を敷いている。暗いアプリ内では線だけで見えるが、
  Explorer の白背景に 16px で置くと細い青線がほぼ消えるため
- Windows は Explorer では **exe 埋め込み**、タスクバーでは**ウィンドウ**のアイコンを見る。
  別々だと起動した瞬間にアイコンが変わるので、両方に同じ .ico を使っている
- ソースから起動したときに python.exe としてグループ化されないよう、独自の
  AppUserModelID を宣言している

### 自動アップデートを有効化

`GITHUB_REPO` に `SHIN-DATA-CENTER/Supercut-Extended` を設定した。これまでは未設定で
アップデート確認そのものが無効だった。

### 修正

- **アイコン生成スクリプトが起動即クラッシュしていた。** `QBuffer` に一時オブジェクトの
  `QByteArray` を渡していたため、バッファが指したまま Python 側が回収してしまい
  segfault していた
- **空文字の翻訳がキー名として表示されていた。** `tr()` が真偽値で判定していたため、
  意図的に空にした文字列（チェック欄の見出しなど）が素通りして `col.pick` のような
  内部キーが出ていた。キーの有無で判定するよう修正
- **出力方法のラジオが 1 回目の切り替えで効かなかった。** Qt は先に旧ラジオを OFF に
  してから新ラジオを ON にするため、片方の `toggled` だけに繋いでいると「どちらも OFF」の
  瞬間に走って古い状態を読んでいた
- **一覧のチェック印が Windows 標準の見た目のまま浮いていた。** アイテムのチェックは
  QCheckBox ではなくビューが描くので、`QCheckBox::indicator` の指定が届いていなかった。
  `QTableWidget::indicator` を追加してアプリ内の他のチェックと揃えた
- **`全選択` / `解除` ボタンの文字がつぶれていた。** 高さを固定していたため、縮むのではなく
  文字が切れていた。固定をやめて他のボタンと同じ高さに揃えた
- **対象イベントの件数がプレビュー中の 1 動画分しか出ていなかった。** 選択した全試合を
  合算して表示するようにした。チェックを増減しても、選んでいたイベント種別は引き継がれる
- **単体 exe で自己アップデートが失敗しうる問題。** 1 ファイル構成では自プロセスの終了後も
  ブートローダの親プロセスが一瞬 exe を掴んでいるため、上書きのリトライ幅を広げた

### 内部

- `render()` を複数ソース対応の `render_many()` / `render_each()` に一般化した。
  単一ソースの経路は `render_many()` にジョブ 1 件を渡す形にしてあり、**セグメント算出の
  結果は従来と完全に一致**する（回帰確認済み）
- 検証スクリプトを追加・更新
  - `tools/test_multiselect.py`（新規） — 複数選択 UI の状態遷移を実際に操作して検証
  - `tools/test_gui_render.py` — 単一 / 結合 / 個別の 3 経路を検証するよう拡張

---

## v1.0.0

初回リリース。

Outplayed のイベント検出と録画はそのまま使い、**結合・エンコードの工程だけを GPU (NVENC) で
置き換える**外部ツール。

- Outplayed の IndexedDB から試合とイベントを読み取る（実行中でも安全にスナップショット）
- セグメント算出は Outplayed の `IOPlugin.createMontage` への実引数と **0.0000 ms 一致**
- NVENC による 2 段レンダリング
  - `encode` — Outplayed 同等の再エンコード。フレーム単位で正確
  - `copy` — 無劣化・高速。カットは約 1 秒単位（素材が 1 秒ごとにキーフレームを持つため）
- GUI: 動画プレビュー、イベント表示兼シークバー、イベント種別の絞り込み、進捗、キャンセル
- CLI: `list` / `events` / `build` / `encoders`
- 日本語（既定）/ English
- GitHub Releases 経由の更新確認・自己更新

### なぜ作ったか

同一条件（25 セグメント / 687.7 秒）での実測:

| | 所要時間 | 結果 |
|---|---|---|
| Outplayed (libx264 / CPU) | 302 秒 | **失敗** — 5 分でタイムアウトし 317.95 秒で打ち切り |
| 本ツール `encode` (NVENC) | **160 秒** | 687.84 秒 完走 |
| 本ツール `copy` | **5.7 秒** | 702.16 秒 完走・無劣化 |

Outplayed の Supercut だけが `libx264`（CPU）で、しかも `-pix_fmt` の指定漏れで
`yuv444p` になっていた。録画（`obs_nvenc_h264_tex`）も手動エクスポート（`h264_nvenc`）も
GPU を使えているのに、Supercut だけが取り残されていた。
